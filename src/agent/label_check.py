"""Phase 51 — ป้ายราคาของ LLM ขัดกับเครื่องยนต์หรือเปล่า.

**ปัญหาที่วัดได้จริงก่อนเขียนไฟล์นี้** (จากประวัติ 780 คู่วันติดกัน / 597 แถวที่มีทั้งสองฝั่ง):
- ป้าย `valuation_view` ของ LLM เปลี่ยน 127 ครั้ง และ **88% ของการเปลี่ยนนั้น คะแนน
  เครื่องยนต์ไม่ขยับเลยแม้แต่น้อย** — มีแค่ 8 ครั้งที่ราคาหุ้นขยับเกิน 5% ด้วย
- ขัดกันคนละขั้วชัดเจน **68 จาก 597 แถว (11.4%)**
- เคสตัวอย่าง DUOL 2026-09-23 -> 24: ราคาขยับ 1.7% ป้ายพลิก cheap -> expensive ขณะที่
  เครื่องยนต์ให้ **3.0/3 ทั้งสองวัน** (เต็มสเกล = ถูกที่สุดเท่าที่ให้ได้)

นี่คือโหมดพังแบบเดียวกับที่ Phase 19.3.1 เคยจับได้กับ sentiment (เป็นตัวขับ 57.4% ของรอบที่
คะแนนกระโดด >=0.5) แล้วตัดออกจากคะแนน — ต่างกันแค่ตอนนั้นมันอยู่ในคะแนนเลยมีคนวัด ส่วนอันนี้
อยู่แค่บนหน้าจอ เลยไม่มีใครจับได้จนถึงวันนี้

**ไฟล์นี้ไม่ตัดสินว่าใครถูก** — ความเห็นที่ขัดกับตัวเลขมีค่าได้ถ้ามันรู้ตัวและอธิบายได้
หน้าที่ของมันคือทำให้ "ขัดกัน" มองเห็น แทนที่จะปล่อยให้สองป้ายวางเคียงกันด้วยน้ำหนักเท่ากัน
โดยไม่มีอะไรบอกว่ามันเถียงกันอยู่
"""

# คะแนนขาราคา 0.0-3.0 -> ป้ายเทียบเคียง
#
# เลือกจุดตัดแบบสมมาตรรอบกลางสเกล (1.5) ไม่ได้ปรับให้พอดีกับข้อมูลที่มีอยู่: 3.0 คือถูกสุด
# 0.0 คือแพงสุด ดังนั้น "ถูก" = ครึ่งบนของครึ่งบน, "แพง" = ครึ่งล่างของครึ่งล่าง ที่เหลือกลางๆ
CHEAP_AT = 2.5
EXPENSIVE_AT = 0.5

_RANK = {"cheap": 2, "fair": 1, "expensive": 0}


def engine_view(score: float | None) -> str | None:
    """แปลคะแนน /3 เป็นป้ายภาษาเดียวกับ LLM — None ถ้าเครื่องยนต์คำนวณไม่ได้
    (ซึ่ง **ไม่ใช่** เหตุผลที่จะบอกว่า LLM ผิด: ไม่มีอะไรให้เทียบ ก็ไม่มีข้อขัดแย้ง)"""
    if score is None:
        return None
    if score >= CHEAP_AT:
        return "cheap"
    if score <= EXPENSIVE_AT:
        return "expensive"
    return "fair"


def valuation_conflict(view: str | None, score: float | None) -> dict | None:
    """คืนรายละเอียดความขัดแย้ง — None ถ้าไม่ขัด หรือเทียบไม่ได้.

    `unclear` ของ LLM ไม่นับว่าขัด — มันคือการบอกว่า "ตอบไม่ได้" ซึ่งเป็นคำตอบที่ซื่อสัตย์
    คนละเรื่องกับการตอบผิดทาง

    แยกสองระดับ เพราะอ่านแล้วต้องทำคนละอย่าง:
    - `opposite`  : ฝั่งตรงข้ามกันเลย (cheap vs expensive) = อย่างน้อยหนึ่งฝั่งผิดแน่
    - `off_by_one`: ห่างกันขั้นเดียว (cheap vs fair) = ต่างมุมมอง ไม่ใช่ขัดแย้ง ไม่ต้องตกใจ
    """
    if view is None or view == "unclear":
        return None
    eng = engine_view(score)
    if eng is None or eng == view:
        return None

    distance = abs(_RANK[view] - _RANK[eng])
    return {
        "llm_view": view,
        "engine_view": eng,
        "engine_score": score,
        "level": "opposite" if distance >= 2 else "off_by_one",
        "note": (f"LLM ว่า “{_TH[view]}” แต่เครื่องยนต์ให้ {score}/3 ซึ่งอ่านว่า “{_TH[eng]}”"),
    }


_TH = {"cheap": "ถูก", "fair": "พอดี", "expensive": "แพง"}
