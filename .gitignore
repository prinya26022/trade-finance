# --- Secrets (สำคัญสุด: อย่าให้ API key หลุด) ---
.env
.env.*
!.env.example
*.key

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Virtual env ---
.venv/
venv/
env/

# --- Data / DB (เก็บโครงโฟลเดอร์ไว้ด้วย .gitkeep แต่ไม่เก็บไฟล์ db) ---
data/*.db
data/*.sqlite
data/*.sqlite3
data/sec_cik_map.json
data/sec_facts_cache/
data/risk_free_rate_cache.json
# Phase 45: เรตค่าเงินก็เป็น cache รายวันแบบเดียวกัน — ค่าตลาด ไม่ใช่ข้อมูลของโปรเจกต์
data/fx_cache.json
# Phase 49: สแนปช็อต yfinance ของเรดาร์ AI-capex (TTL 6 ชม.) — คำนวณใหม่ได้เสมอ
data/aicapex_cache.json
data/screener_cache.json
data/price_cache.json
# ไฟล์ที่ export ไปแปะในแชท + คำตอบที่ได้กลับมา (Phase 33) — สร้างใหม่ได้จาก DB/providers
# และก้อนใหญ่ (65KB/งวด); ของจริงที่ต้องเก็บคือแถวใน claude_analyses ซึ่งอยู่ใน DB แล้ว
data/claude_packs/
data/onchain_cache/

# --- Node / Next.js (web/) ---
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# --- Logs (จาก scheduled run) ---
logs/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.idea/
*.swp

